<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.bradleyboxer.scavengerhunt">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_LOCATION_EXTRA_COMMANDS" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".v3.TextInputActivity"
            android:label="Text Input"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name=".v3.CompassActivity"
            android:label="@string/title_activity_solve_compass_clue"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".v3.ClueViewActivity"
            android:label="@string/title_activity_clue_view"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".v3.MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".v3.GeofenceTransitionsIntentService" />
    </application>

</manifest>